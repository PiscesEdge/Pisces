<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">
            <img src="/public/img/VM Logo.png" alt="logo">
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTxt"
            aria-controls="navbarTxt">
            <span class=" navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarTxt">
            <ul class="navbar-nav mr-auto mb-2 mb-lg-0 d-flex">
                <li class="nav-item">
                    <% if (title=="Home" ) {%>
                        <a class="nav-link active" aria-current="page" href="/">Home</a>
                        <%} else {%>
                            <a class="nav-link" aria-current="page" href="/">Home</a>
                            <%} %>
                </li>
                <li class="nav-item">
                    <% if (title=="About" ) {%>
                        <a class="nav-link active" aria-current="/about" href="/">About</a>
                        <%} else {%>
                            <a class="nav-link" href="/about">About</a>
                            <%} %>
                </li>
                <li class="nav-item">
                    <% if (title=="Projects" ) {%>
                        <a class="nav-link active" aria-current="/projects" href="/">Projects</a>
                        <%} else {%>
                            <a class="nav-link" href="/projects">Projects</a>
                            <%} %>
                </li>
                <li class="nav-item">
                    <% if (title=="Services" ) {%>
                        <a class="nav-link active" aria-current="/services" href="/">Services</a>
                        <%} else {%>
                            <a class="nav-link" href="/services">Services</a>
                            <%} %>
                </li>
                <li class="nav-item">
                    <% if (title=="Contact" ) {%>
                        <a class="nav-link active" aria-current="/contact" href="/">Contact</a>
                        <%} else {%>
                            <a class="nav-link" href="/contact">Contact</a>
                            <%} %>
                </li>
        </div>
    </div>
</nav>